# FastAPI web framework
fastapi>=0.100.0
uvicorn>=0.20.0
pydantic>=2.0.0

# Core dependencies
requests>=2.28.0
python-dotenv>=1.0.0

# AliExpress API integration
python-aliexpress-api>=1.0.0

# Async and caching support (with Python 3.12 compatibility)
# Note: aioredis is deprecated, use redis[asyncio] instead
# aioredis>=2.0.1,<3.0.0  # Removed - has native dependencies that fail on Vercel
redis>=4.5.0  # Pure Python, Vercel-compatible
asyncio-throttle>=1.0.0

# Security and authentication
PyJWT>=2.8.0
python-jose[cryptography]>=3.3.0
cryptography>=41.0.0