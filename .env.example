# AliExpress API Configuration (Required)
ALIEXPRESS_APP_KEY=520934
ALIEXPRESS_APP_SECRET=inC2NFrIr1SvtTGlUWxyQec6EvHyjIno 
ALIEXPRESS_TRACKING_ID=gpt_chat
ALIEXPRESS_LANGUAGE=EN
ALIEXPRESS_CURRENCY=USD

# API Server Configuration
API_HOST=0.0.0.0
API_PORT=8000
LOG_LEVEL=INFO

# Cache Configuration (for optimal API call reduction)
# These settings enable 70-90% reduction in API calls
ENABLE_REDIS_CACHE=true
ENABLE_DB_CACHE=true
CACHE_PRODUCT_TTL=86400
CACHE_AFFILIATE_TTL=2592000
CACHE_SEARCH_TTL=3600
CACHE_PRICE_TTL=1800

# Redis Configuration (optional but recommended for production)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=

# Database Configuration for persistent caching
CACHE_DATABASE_URL=sqlite:///cache.db

# Image Processing Configuration
ENABLE_IMAGE_SEARCH=true
IMAGE_PROCESSING_METHOD=clip
MAX_IMAGE_SIZE_MB=10
IMAGE_CACHE_TTL=86400

# Security Configuration
# ⚠️ PRODUCTION WARNING: These keys MUST be changed before deploying to production!
# Generate strong random keys using: python -c "import secrets; print(secrets.token_urlsafe(32))"
ADMIN_API_KEY=admin-secret-key-change-in-production
INTERNAL_API_KEY=ALIINSIDER-2025
JWT_SECRET_KEY=change-this-secret-key-in-production

# Rate Limiting Configuration
# Controls API request throttling to prevent abuse
MAX_REQUESTS_PER_MINUTE=60
MAX_REQUESTS_PER_SECOND=5

# CORS and Deployment Configuration
# Comma-separated list of allowed origins for CORS
ALLOWED_ORIGINS=https://chat.openai.com,https://chatgpt.com
# Environment: development, staging, or production
ENVIRONMENT=development
# Enable debug mode (disable in production)
DEBUG=false
# Enable HTTPS redirect (enable in production)
ENABLE_HTTPS_REDIRECT=false
# Production domain for Vercel deployment
PRODUCTION_DOMAIN=alistach.vercel.app